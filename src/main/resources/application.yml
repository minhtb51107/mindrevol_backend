# Cấu hình pipeline mặc định
app:
  default-pipeline-name: "default-rag"

# Loại bỏ cấp "definitions" thừa
pipelines:
  default-rag:
    steps:
      - retrieval
      - reranking
      - generation
  simple-rag:
    steps:
      - retrieval
      - generation

# ... các cấu hình khác của bạn