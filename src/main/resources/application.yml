pipelines:
  definitions: # <-- THÊM DÒNG NÀY
    # Tên pipeline (lùi vào 1 cấp)
    default-rag:
      # Danh sách các step theo đúng thứ tự thực thi
      steps:
        - retrieval
        - reranking
        - generation
    # Một pipeline khác để thử nghiệm (lùi vào 1 cấp)
    simple-rag:
      steps:
        - retrieval
        - generation